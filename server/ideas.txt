Database Schema ideas


For user:
    Username string
    password string(hash?)
    connection to file db


For file:
    title string
    content ?
    location to be stored?



connect wiki code handlers with new handlers for app



how to check if bucket name is taken


Page struct to display each file?

Bucket struct
display name
S3 bucket name -  random name
list of files

File struct
display name
actual file


mongoDb in Go?



end of tomorrow: login in, register with mongo, routing for api, start some frontend



Questions:
    How to use the getter functions in other functions, etc?

    User struct can be used as the model? Do we want to store files on DB as well then?

    User struct has a bucket slice with type of Bucket.. Where are the files going then?

    
